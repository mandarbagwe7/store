spring:
  application:
    name: store
  datasource:
    url: jdbc:mysql://localhost:3306/store?createDatabaseIfNotExist=true
    username: root
    password: root
stripe:
  apiurl: https://www.stripe.com
  enabled: true
  timeout: 1000
  supported-currencies: INR,USD,EUR
payment-gateway: stripe
email:
  host: localhost
  port: 8080